- Open project in Pycharm and run the following in the terminal: "jupyter lab". A website should open with the code.

- When jypterlab is opened, click ">>" so that it restarts the program and runs all cells.